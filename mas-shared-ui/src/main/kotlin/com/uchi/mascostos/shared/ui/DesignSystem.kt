package com.uchi.mascostos.shared.ui

object MasDesignSystem {
    object Spacing {
        const val section = 10.0
        const val card = 12.0
    }

    object StatusText {
        const val parityAligned = "Paridad UI con Desktop: alineada ✅"
        const val pendingRoadmap = "Pendiente: async/loading, reintentos/cache/auth, PDF y validaciones"
    }
}
